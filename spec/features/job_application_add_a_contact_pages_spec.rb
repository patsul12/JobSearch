require 'rails_helper'

describe 'the add a contact to a job application path' do
  it 'allows a user to add a contact to an existing job application', js: true do
    # company = create(:company)
    # user = create(:user_with_application_and_contacts)
    # login_as(user)
    # visit root_path
    # click_link ('job-application-show-toggle-' + user.job_applications.first.id.to_s)
    # click_link 'Add a Contact'
    # click_link '+'
    # expect(page).to have_content("Contact added successfuly");
  end
end
